package server

import (
	"testing"
)

func TestHealthCheckEndpoint(t *testing.T) {
	// Use the improved test that uses temporary directories
	t.Skip("Replaced by TestHealthCheckEndpointImproved to avoid index lock issues")
}

func TestHealthCheckUptime(t *testing.T) {
	// Use the improved test that uses temporary directories
	t.Skip("Replaced by TestHealthCheckUptimeImproved to avoid index lock issues")
}

func TestHealthCheckWithSessions(t *testing.T) {
	// Use the improved test that uses temporary directories
	t.Skip("Replaced by TestHealthCheckWithSessionsImproved to avoid index lock issues")
}